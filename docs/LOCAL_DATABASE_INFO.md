# 本地数据库位置信息

## 📍 本地开发数据库位置

### Windows 路径

**完整路径**:
```
C:\App\NUEVO\.wrangler\state\v3\d1\miniflare-D1DatabaseObject\
```

**相对路径**:
```
.wrangler/state/v3/d1/miniflare-D1DatabaseObject/
```

## 📁 目录结构

```
.wrangler/
└── state/
    └── v3/
        └── d1/
            └── miniflare-D1DatabaseObject/
                └── [数据库文件]
```

## 🔍 数据库文件说明

### 文件位置
- **目录**: `.wrangler/state/v3/d1/miniflare-D1DatabaseObject/`
- **文件类型**: SQLite 数据库文件
- **文件格式**: `.sqlite` 或 `.db` 文件

### 重要提示

1. **自动管理**: 这些文件由 Wrangler 自动创建和管理
2. **开发专用**: 仅用于本地开发，不会影响云端数据库
3. **可以删除**: 删除后，下次运行 `wrangler dev` 时会自动重新创建
4. **不提交**: `.wrangler/` 目录已在 `.gitignore` 中，不会被提交到 Git

## 🔧 查看本地数据库

### 方法 1: 使用 Wrangler CLI

```bash
# 查看所有表
npx wrangler d1 execute jewelry-db --command="SELECT name FROM sqlite_master WHERE type='table'"

# 查看商品
npx wrangler d1 execute jewelry-db --command="SELECT * FROM products"

# 查看分类
npx wrangler d1 execute jewelry-db --command="SELECT * FROM categories"
```

### 方法 2: 直接查看文件

如果安装了 SQLite 工具，可以直接打开数据库文件：

```bash
# 使用 sqlite3 命令行工具
sqlite3 ".wrangler/state/v3/d1/miniflare-D1DatabaseObject/[数据库文件名]"

# 或者使用 SQLite 图形工具（如 DB Browser for SQLite）
# 打开文件: .wrangler/state/v3/d1/miniflare-D1DatabaseObject/[数据库文件名]
```

## 📊 本地 vs 云端数据库

| 特性 | 本地数据库 | 云端数据库 |
|------|-----------|-----------|
| 位置 | `.wrangler/state/v3/d1/` | Cloudflare 云端 |
| 用途 | 本地开发测试 | 生产数据 |
| 同步 | 手动同步 | 主要数据源 |
| 访问速度 | 非常快 | 通过网络 |
| 数据持久化 | 本地文件 | 云端存储 |

## 🗑️ 清理本地数据库

如果需要重置本地数据库：

```bash
# 停止开发服务器
# 删除本地数据库目录
Remove-Item -Recurse -Force ".wrangler\state\v3\d1"

# 重新启动开发服务器，会自动创建新的本地数据库
npm run wrangler:dev
```

## 📝 注意事项

1. **不要手动修改**: 本地数据库文件由 Wrangler 管理，不要手动编辑
2. **备份重要**: 如果需要备份，使用 Wrangler CLI 导出数据
3. **Git 忽略**: `.wrangler/` 目录已在 `.gitignore` 中
4. **开发专用**: 本地数据库仅用于开发，生产环境使用云端数据库

## 🔄 同步数据

### 从云端同步到本地
```bash
# 运行迁移（会同步到本地）
npm run db:migrate
```

### 从本地同步到云端
```bash
# 运行迁移到远程
npx wrangler d1 execute jewelry-db --file=./migrations/0001_initial_schema.sql --remote
```

## ✨ 总结

- **本地数据库位置**: `C:\App\NUEVO\.wrangler\state\v3\d1\miniflare-D1DatabaseObject\`
- **用途**: 本地开发测试
- **管理**: 由 Wrangler 自动管理
- **访问**: 通过 Wrangler CLI 或 API

本地数据库文件就在这里！🎯

